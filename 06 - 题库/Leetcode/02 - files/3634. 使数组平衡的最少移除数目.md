---
难度: medium
情况: Y(通过)
日期: 2026-02-15 22:09
分类: 滑动窗口,二分
tags:
  - 每日一题
备注: No
created: 2026-02-23T20:45
updated: 2026-02-26T10:43
---

> 🔗[3634 - 使数组平衡的最少移除数目 ](https://leetcode.cn/problems/minimum-removals-to-balance-array/description/?envType=daily-question&envId=2026-02-15)

## 📌 题目描述
>   （请在此处粘贴题目描述）

给你一个整数数组 `nums` 和一个整数 `k`。

如果一个数组的 **最大** 元素的值 **至多** 是其 **最小** 元素的 `k` 倍，则该数组被称为是 **平衡** 的。

你可以从 `nums` 中移除 **任意** 数量的元素，但不能使其变为 **空** 数组。

返回为了使剩余数组平衡，需要移除的元素的 **最小** 数量。

**注意：** 大小为 1 的数组被认为是平衡的，因为其最大值和最小值相等，且条件总是成立。

**示例 1:**

**输入：** nums = [2,1,5], k = 2

**输出：** 1

**解释：**

- 移除 `nums[2] = 5` 得到 `nums = [2, 1]`。
- 现在 `max = 2`, `min = 1`，且 `max <= min * k`，因为 `2 <= 1 * 2`。因此，答案是 1。

**示例 2:**

**输入：** nums = [1,6,2,9], k = 3

**输出：** 2

**解释：**

- 移除 `nums[0] = 1` 和 `nums[3] = 9` 得到 `nums = [6, 2]`。
- 现在 `max = 6`, `min = 2`，且 `max <= min * k`，因为 `6 <= 2 * 3`。因此，答案是 2。

**示例 3:**

**输入：** nums = [4,6], k = 2

**输出：** 0

**解释：**

- 由于 `nums` 已经平衡，因为 `6 <= 4 * 2`，所以不需要移除任何元素。

**提示：**

- $1 <= nums.length <= 10^5$
- $1 <= nums[i] <= 10^9$
- $1 <= k <= 10^5$

## 💡 解题思路 
首先对数组进行**排序**，枚举最大值 $mx=nums[i]$，则最小值 $mn=nums[left]$ 必须满足：
$$ k * mn \ge mx $$
在 $[mn,mx]$ 中的元素保留，其余的元素则去掉；如果不满足上式，则 $left$ 加一；遍历 $i$ 的过程中，更新 $res=max(res, n - maxSave)$。

## 💻 代码实现
>  my answer
```cpp
class Solution {
public:
    int minRemoval(vector<int>& nums, int k) {
        int n = nums.size();
        if (n == 1) return 0;
        sort(nums.begin(), nums.end());
        int minDis = n;
        for (int p = 0; p < n; p++) {
            // 枚举最小值, 二分最右边的满足条件的数
            int i = p, j = n - 1, dis = 0, mid = 0;
            while (i < j) {
                mid = (i + j + 1) >> 1;
                if (nums[mid] <= (long long)k * nums[p]) i = mid;
                else j = mid - 1;
            }
            if (nums[mid] > (long long)k * nums[p]) dis = n - (mid - p);
            else dis = n - (mid - p + 1); // 边界的处理
            minDis = min(minDis, dis);
        }
        return minDis;
    }
};
```

- **时间复杂度**：`O(nlogn)`。排序的时间复杂度
- **空间复杂度**：`O(logn)`。排序的空间复杂度


> to optimize

使用**二分**也能通过，但是边界处理过多，不够优雅。
滑动窗口：
```cpp
class Solution {
public:
    int minRemoval(vector<int>& nums, int k) {
        ranges::sort(nums);
        int max_save = 0, left = 0;
        for (int i = 0; i < nums.size(); i++) {
            while (1LL * nums[left] * k < nums[i]) {
                left++;
            }
            max_save = max(max_save, i - left + 1);
        }
        return nums.size() - max_save;
    }
};

作者：灵茶山艾府
链接：https://leetcode.cn/problems/minimum-removals-to-balance-array/solutions/3740886/pai-xu-hua-dong-chuang-kou-pythonjavacgo-pfho/
来源：力扣（LeetCode）
著作权归作者所有。商业转载请联系作者获得授权，非商业转载请注明出处。
```

- **时间复杂度**：`O(nlogn)`
- **空间复杂度**：`O(logn)`。排序的空间复杂度平均为 `logn`

>✨ tips 

  由于 $k * nums[i]$ 最大为 $10^9$，所以使用 `long long` 保证不溢出，也可以选择 `1LL * nums[i] * k` 则类型提升至 `long long`
```cpp
while (1LL * nums[left] * k < nums[i]) {
    left++;
}
```

> others' answer

todo:

- **时间复杂度**：`O(...)`
- **空间复杂度**：`O(...)`

>✨ tips




## 🔁 复盘与扩展
> 关键点：
-  排序使用滑动窗口
-  乘法溢出，超过 `int` 可以表示的最大值

> 易错点：
-  乘法**溢出**，超过 `int` 可以表示的最大值


## 🔗 相关题目




> ⏱️ 本次耗时：______ __30__ 分钟





