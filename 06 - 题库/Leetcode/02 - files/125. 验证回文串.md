---
created: 2026-03-07T10:18
updated: 2026-06-15T17:05
难度: easy
情况: Y(通过)
日期: 2026-03-07 10:18
分类: 双指针, 模拟
tags:
  - 面试经典150题
备注:
---

> 🔗[125 - 验证回文串](https://leetcode.cn/problems/valid-palindrome/description/?envType=study-plan-v2&envId=top-interview-150)

## 📌 题目描述
> （请在此处粘贴题目描述）

如果在将所有大写字符转换为小写字符、并移除所有非字母数字字符之后，短语正着读和反着读都一样。则可以认为该短语是一个 **回文串** 。

字母和数字都属于字母数字字符。

给你一个字符串 `s`，如果它是 **回文串** ，返回 `true` ；否则，返回 `false` 。

**示例 1：**

**输入:** s = "A man, a plan, a canal: Panama"
**输出：** true
**解释：**"amanaplanacanalpanama" 是回文串。

**示例 2：**

**输入：** s = "race a car"
**输出：** false
**解释：**"raceacar" 不是回文串。

**示例 3：**

**输入：** s = " "
**输出：** true
**解释：** 在移除非字母数字字符之后，s 是一个空字符串 "" 。
由于空字符串正着反着读都一样，所以是回文串。

**提示：**

- $1 <= s.length <= 2 * 10^5$
- `s` 仅由可打印的 ASCII 字符组成

## 💡 解题思路 
初始化两个指针`i = 0, j = s.size() - 1`，每次跳过非字母数字符。比较`i`和`j` 所指的字符是否相等，若不等则返回`false`；直到`i >= j` 若没返回`false`则返回`true`。

## 💻 代码实现
> my answer
```cpp
class Solution {
public:
    bool check(char c) {
        bool res1 = (c >= 'a' && c <= 'z');
        bool res2 = (c >= 'A' && c <= 'Z');
        return (res1 || res2);
    }
    bool isPalindrome(string s) {
        int i = 0, j = s.size() - 1;
        bool flag = true; // 初始认为是回文串
        while (i < j) {
            while (i < j && (!isdigit(s[i]) && !check(s[i]))) i++;
            while (i < j && (!isdigit(s[j]) && !check(s[j]))) j--;
            if (isdigit(s[i]) && check(s[j])) {
                flag = false;
                break;
            } else if (isdigit(s[i]) && isdigit(s[j])) {
                if (s[i] != s[j]) {
                    flag = false;
                    break;
                }
            } else if (check(s[i]) && isdigit(s[j])) {
                flag = false;
                break;
            } else if (check(s[i]) && check(s[j])) {
                if (tolower(s[i]) != tolower(s[j])) {
                    flag = false;
                    break;
                }
            }
            i++;
            j--;
        }
        if (flag) return true;
        else return false;
    }
};
```
- **时间复杂度**：`O(n)`。$n$ 是字符串`s`的长度。
- **空间复杂度**：`O(1)`

> to optimize

`<cctype>`中有专门判断是否是字母数字字符的函数`isalnum`。
```cpp
class Solution {
public:
    bool isPalindrome(string s) {
        int i = 0, j = s.size() - 1;
        while (i < j) {
            if (!isalnum(s[i])) {
                i++;
            } else if (!isalnum(s[j])) {
                j--;
            } else if (tolower(s[i]) == tolower(s[j])) {
                i++;
                j--;
            } else {
                return false;
            }
        }
        return true;
    }
};
```
- **时间复杂度**：`O(n)`
- **空间复杂度**：`O(1)`

>✨ tips
- `isalnum`和`tolower`介绍见[[isalnum]]
> others' answer

- **时间复杂度**：`O(...)`
- **空间复杂度**：`O(...)`

>✨ tips

## 🔁 复盘与扩展
> 关键点：

> 易错点：

## 🔗 相关题目


> ⏱️ 本次耗时：____ 分钟
