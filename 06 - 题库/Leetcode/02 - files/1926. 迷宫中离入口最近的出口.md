---
难度: medium
情况: ?(有疑问)
日期: 2026-03-12 13:28
分类: 搜索
tags:
  - leetcode
备注: 单源bfs
created: 2026-03-12T13:28
updated: 2026-03-14T11:26
---

> 🔗[1926 - 迷宫中离入口最近的出口](https://leetcode.cn/problems/nearest-exit-from-entrance-in-maze/description/)

## 📌 题目描述
> （请在此处粘贴题目描述）

给你一个 `m x n` 的迷宫矩阵 `maze` （**下标从 0 开始**），矩阵中有空格子（用 `'.'` 表示）和墙（用 `'+'` 表示）。同时给你迷宫的入口 `entrance` ，用 `entrance = [entrancerow, entrancecol]` 表示你一开始所在格子的行和列。

每一步操作，你可以往 **上**，**下**，**左** 或者 **右** 移动一个格子。你不能进入墙所在的格子，你也不能离开迷宫。你的目标是找到离 `entrance` **最近** 的出口。**出口** 的含义是 `maze` **边界** 上的 **空格子**。`entrance` 格子 **不算** 出口。

请你返回从 `entrance` 到最近出口的最短路径的 **步数** ，如果不存在这样的路径，请你返回 `-1` 。

**示例 1：**

![](https://assets.leetcode.com/uploads/2021/06/04/nearest1-grid.jpg)

**输入：** maze = `[["+","+",".","+"],[".",".",".","+"],["+","+","+","."]], entrance = [1,2]`
**输出：** 1
**解释：** 总共有 3 个出口，分别位于 (1,0)，(0,2) 和 (2,3) 。
一开始，你在入口格子 (1,2) 处。
- 你可以往左移动 2 步到达 (1,0) 。
- 你可以往上移动 1 步到达 (0,2) 。
从入口处没法到达 (2,3) 。
所以，最近的出口是 (0,2) ，距离为 1 步。

**示例 2：**

![](https://assets.leetcode.com/uploads/2021/06/04/nearesr2-grid.jpg)

**输入：** maze =`[["+","+","+"],[".",".","."],["+","+","+"]], entrance = [1,0]`
**输出：** 2
**解释：** 迷宫中只有 1 个出口，在 (1,2) 处。
(1,0) 不算出口，因为它是入口格子。
初始时，你在入口与格子 (1,0) 处。
- 你可以往右移动 2 步到达 (1,2) 处。
所以，最近的出口为 (1,2) ，距离为 2 步。

**示例 3：**

![](https://assets.leetcode.com/uploads/2021/06/04/nearest3-grid.jpg)

**输入：** maze = `[[".","+"]], entrance = [0,0]`
**输出：**-1
**解释：** 这个迷宫中没有出口。

**提示：**

- `maze.length == m`
- `maze[i].length == n`
- `1 <= m, n <= 100`
- `maze[i][j]` 要么是 `'.'` ，要么是 `'+'` 。
- `entrance.length == 2`
- `0 <= entrancerow < m`
- `0 <= entrancecol < n`
- `entrance` 一定是空格子。

## 💡 解题思路 
使用`bfs`扩展。有以下几个问题？
- 什么时候更新步数`steps`？
	- 当要访问下一层的节点时就得更新。
- 什么时候置已访问标记？
	- 对于起点，在推入队列后就置访问标记。
	- 对于`bfs`扩展的点，一定要先检查是否已经访问过（否则会走重复路），再加入队列，并置访问标记。
- 对于出口的判断一定要在检查当前位置`[x, y]`是否被访问过之后，否则可能会将起点（在边界上）当做出口。
	例如：
	![image.png](https://cdn.jsdelivr.net/gh/uaenaGit/image-host@main/images20260312134350935.png)


## 💻 代码实现
> my answer
```cpp
class Solution {
    static constexpr int d[4][2] = {
        {-1, 0},
        {1, 0},
        {0, -1},
        {0, 1}
    };
public:
    int nearestExit(vector<vector<char>>& maze, vector<int>& entrance) {
        int m = maze.size(), n = maze[0].size();
        queue<pair<int, int>> q; // bfs使用的队列，存储的是方位
        vector<vector<bool>> st(m, vector<bool>(n, false)); // 存储当前方位是否已经访问
        int x = entrance[0], y = entrance[1]; // 起点的方位
        q.push({x, y});
        st[x][y] = true; // 将起点置已访问标记

        int steps = 0;
        while (!q.empty()) {
            steps++; // 进入下一层 步数加1
            int size = q.size();
            // 注意每次必须将当前层遍历完，否则可能走到另一条非最短路径
            for (int k = 0; k < size; k++) {
                auto [i, j] = q.front();
                q.pop();
                for (auto &[dx, dy] : d) {
                    int nx = i + dx, ny = j + dy;
                    // 1. 检查边界和障碍物
                    if (nx < 0 || nx >= m || ny < 0 || ny >= n || maze[nx][ny] == '+') {
                        continue;
                    }

                    // 2. 检查是否已访问（push之前检查）
                    if (st[nx][ny]) {
                        continue;
                    }

                    // 3. 走到边界上看看是不是出口
                    if (nx == 0 || nx == m - 1 || ny == 0 || ny == n - 1) {
                        return steps;
                    }

                    // 4. 置标记并入队
                    st[nx][ny] = true;
                    q.push({nx, ny});
                }
            }
        }
        return -1;
    }
};
```
- **时间复杂度**：`O(mn)`
- **空间复杂度**：`O(mn)`

> to optimize

不使用标记数组。
```cpp
class Solution {
    static constexpr int d[4][2] = {
        {-1, 0},
        {1, 0},
        {0, -1},
        {0, 1}
    };
public:
    int nearestExit(vector<vector<char>>& maze, vector<int>& entrance) {
        int m = maze.size(), n = maze[0].size();
        queue<pair<int, int>> q; // bfs使用的队列，存储的是方位
        int x = entrance[0], y = entrance[1]; // 起点的方位
        q.push({x, y});
        maze[x][y] = '+'; // 置成不能访问

        int steps = 0;
        while (!q.empty()) {
            steps++; // 进入下一层 步数加1
            int size = q.size();
            // 注意每次必须将当前层遍历完，否则可能走到另一条非最短路径
            for (int k = 0; k < size; k++) {
                auto [i, j] = q.front();
                q.pop();
                for (auto &[dx, dy] : d) {
                    int nx = i + dx, ny = j + dy;
                    // 1. 检查边界和障碍物
                    if (nx < 0 || nx >= m || ny < 0 || ny >= n || maze[nx][ny] == '+') {
                        continue;
                    }

                    // 2. 走到边界上看看是不是出口
                    if (nx == 0 || nx == m - 1 || ny == 0 || ny == n - 1) {
                        return steps;
                    }

                    // 3. 置标记并入队
                    q.push({nx, ny});
                    maze[nx][ny] = '+';
                }
            }
        }
        return -1;
    }
};
```
- **时间复杂度**：`O(mn)`
- **空间复杂度**：`O(min(m, n))`。队列中最多保持`min(m, n)`个节点。

>✨ tips

> others' answer

可以额外使用一个`dist`数组来记录起点到各个可以到达的点的距离。这样就不用每次`bfs`遍历的时候都把一层遍历完。
```cpp
class Solution {
    static constexpr int d[4][2] = {
        {-1, 0},
        {1, 0},
        {0, -1},
        {0, 1}
    };
public:
    int nearestExit(vector<vector<char>>& maze, vector<int>& entrance) {
        int m = maze.size(), n = maze[0].size();
        queue<pair<int, int>> q; // bfs使用的队列，存储的是方位
        int x = entrance[0], y = entrance[1]; // 起点的方位
        q.push({x, y});
        maze[x][y] = '+'; // 置成不能访问
        vector<vector<int>> dist(m, vector<int>(n, -1)); // dist数组初始为-1
        dist[x][y] = 0; // 起点距离为0

        while (q.size()) {
            auto [i, j] = q.front();
            q.pop();
            for (auto &[dx, dy] : d) {
                int nx = i + dx, ny = j + dy;
                if (0 <= nx && nx < m && 0 <= ny && ny < n && maze[nx][ny] == '.') {
                    dist[nx][ny] = dist[i][j] + 1;
                    if (nx == 0 || nx == m - 1 || ny == 0 || ny == n - 1) {
                        return dist[nx][ny];
                    }
                    maze[nx][ny] = '+';
                    q.push({nx, ny});
                }
            }
        }
        return -1;
    }
};
```
- **时间复杂度**：`O(mn)`
- **空间复杂度**：`O(mn)`。`dist`数组大小。

>✨ tips

## 🔁 复盘与扩展
> 关键点：

`bfs`网格图通用模版：
```cpp
constexpr int DIRS[4][2] = {{0, -1}, {0, 1}, {-1, 0}, {1, 0}}; // 左右上下

// 返回从 (start_x, start_y) 出发，到其余格子的最短距离
// 时间复杂度 O(mn)
vector<vector<int>> bfsGrid(vector<vector<char>>& grid, int start_x, int start_y) {
    int m = grid.size(), n = grid[0].size();
    vector dis(m, vector<int>(n, -1));
    queue<pair<int, int>> q;

    dis[start_x][start_y] = 0;
    q.emplace(start_x, start_y);

    while (!q.empty()) {
        auto [i, j] = q.front();
        q.pop();
        for (auto [dx, dy] : DIRS) {
            int x = i + dx, y = j + dy;
            // 这里 grid[x][y] == '.' 根据题意修改
            if (0 <= x && x < m && 0 <= y && y < n && grid[x][y] == '.' && dis[x][y] < 0) {
                dis[x][y] = dis[i][j] + 1;
                q.emplace(x, y);
            }
        }
    }

    return dis;
}

作者：灵茶山艾府
链接：https://leetcode.cn/discuss/post/YiXPXW/
来源：力扣（LeetCode）
著作权归作者所有。商业转载请联系作者获得授权，非商业转载请注明出处。
```

> 易错点：

## 🔗 相关题目


> ⏱️ 本次耗时：__25__ 分钟
