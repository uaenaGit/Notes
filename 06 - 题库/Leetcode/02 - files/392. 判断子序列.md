---
难度: easy
情况: Y(通过)
日期: 2026-03-07 10:41
分类: 双指针, 字符串
tags:
  - 面试经典150题
备注:
created: 2026-03-07T10:41
updated: 2026-03-07T11:35
---

> 🔗[392 - 判断子序列](https://leetcode.cn/problems/is-subsequence/description/?envType=study-plan-v2&envId=top-interview-150)

## 📌 题目描述
> （请在此处粘贴题目描述）

给定字符串 **s** 和 **t** ，判断 **s** 是否为 **t** 的子序列。

字符串的一个子序列是原始字符串删除一些（也可以不删除）字符而不改变剩余字符相对位置形成的新字符串。（例如，`"ace"`是`"abcde"`的一个子序列，而`"aec"`不是）。

**进阶：**

如果有大量输入的 S，称作 S1, S2, ... , Sk 其中 k >= 10亿，你需要依次检查它们是否为 T 的子序列。在这种情况下，你会怎样改变代码？

**致谢：**

特别感谢 [@pbrother](https://leetcode.com/pbrother/) 添加此问题并且创建所有测试用例。

**示例 1：**

**输入：** s = "abc", t = "ahbgdc"
**输出：** true

**示例 2：**

**输入：** s = "axc", t = "ahbgdc"
**输出：** false

**提示：**

- `0 <= s.length <= 100`
- $0 <= t.length <= 10^4$
- 两个字符串都只由小写字符组成。

## 💡 解题思路 
使用双指针遍历即可，初始指针`i`和`j` 分别指向`t` 和`s`的首位。
## 💻 代码实现
> my answer
```cpp
class Solution {
public:
    bool isSubsequence(string s, string t) {
        if (s.size() > t.size()) return false;
        int i = 0, j = 0; // i 和 j 分别指向 t 和 s 的首位
        while (i < t.size()) {
            if (t[i] == s[j]) {
                i++;
                j++;
            } else {
                i++;
            }
            if (j == s.size()) {
                break;
            }
        }
        if (j < s.size()) return false;
        return true;
    }
};
```
- **时间复杂度**：`O(n)`
- **空间复杂度**：`O(1)`

> to optimize

如果有大量输入的 S，称作 S1, S2, ... , Sk 其中 k >= 10亿，你需要依次检查它们是否为 T 的子序列。在这种情况下，你会怎样改变代码？
![image.png](https://cdn.jsdelivr.net/gh/uaenaGit/image-host@main/images20260307112017704.png)
![image.png](https://cdn.jsdelivr.net/gh/uaenaGit/image-host@main/images20260307112120332.png)
```cpp
class Solution {
public:
    bool isSubsequence(string s, string t) {
        int n = t.size();
        vector<vector<int>> nxt(n + 1, vector<int>(26));
        fill(nxt[n].begin(), nxt[n].begin() + 26, n); // 初始化为nxt[n][c] = n;
        // 预处理 nxt 数组
        for (int i = n - 1; i >= 0; i--) {
            nxt[i] = nxt[i + 1]; // 等价于 t[i] != c, nxt[i][c] = nxt[i + 1][c];
            nxt[i][t[i] - 'a'] = i; // 如果 t[i] == c, nxt[i][c] = i;
        }
        int i = -1;
        for (auto &c : s) {
            i = nxt[i + 1][c - 'a']; // 得到 i 之后应从 i 的下一个位置查询
            if (i == n) {
                return false;
            }
        }
        return true;
    }
};
```


- **时间复杂度**：`O(n|∑| + km)`。`n` 是 `t` 的长度，`|∑| = 26` 是字符集合的大小； `m` 是 `si`的平均长度，`k`是 `s` 的个数。
- **空间复杂度**：`O(n|∑|)`。

>✨ tips
- 由于涉及到`nxt[i][c] = nxt[i + 1][c]`所以得倒序初始化 `nxt`数组。
- `i`初始化为`-1`这样可计算`nxt[0][c]`的值。
> others' answer

- **时间复杂度**：`O(...)`
- **空间复杂度**：`O(...)`

>✨ tips

## 🔁 复盘与扩展
> 关键点：

> 易错点：

## 🔗 相关题目


> ⏱️ 本次耗时：____ 分钟
