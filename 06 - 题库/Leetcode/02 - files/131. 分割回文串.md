---
难度: medium
情况: N(不会)
日期: 2026-03-13 19:09
分类: 回溯, 递归, 搜索
tags:
  - leetcode
备注: 子集型回溯
created: 2026-03-13T19:09
updated: 2026-03-22T11:00
---

> 🔗[131 - 分割回文串](https://leetcode.cn/problems/palindrome-partitioning/)

## 📌 题目描述
> （请在此处粘贴题目描述）

给你一个字符串 `s`，请你将 `s` 分割成一些 子串，使每个子串都是 **回文串** 。返回 `s` 所有可能的分割方案。

**示例 1：**

**输入：** s = "aab"
**输出：**`[["a","a","b"],["aa","b"]]`

**示例 2：**

**输入：** s = "a"
**输出：**`[["a"]]`

**提示：**

- `1 <= s.length <= 16`
- `s` 仅由小写英文字母组成

## 💡 解题思路 
注意：是枚举**回文子串结束**位置。比如下图中，下一个子问题就得从`j + 1` 考量
![image.png](https://cdn.jsdelivr.net/gh/uaenaGit/image-host@main/images20260313191629383.png)

## 💻 代码实现
> my answer
```cpp
class Solution {
public:
    bool check(const string& s, int left, int right) {
        while (left < right) {
            if (s[left++] != s[right--]) {
                return false;
            }
        }
        return true;
    }
    vector<vector<string>> partition(string s) {
        int n = s.size();
        vector<vector<string>> ans;
        vector<string> path;

        auto dfs = [&](this auto &&dfs, int i) ->void {
            if (i == n) { // 分割结束 加入答案
                ans.push_back(path);
                return;
            }
            for (int j = i; j < n; j++) { // 枚举子串的结束位置
                if (check(s, i, j)) {
                    // 是回文串
                    path.push_back(s.substr(i, j-i+1)); // 分割 (不可能是空串)
                    // 考虑剩余的 s[j+1] ~ s[n-1]怎么分割
                    dfs(j+1);
                    path.pop_back(); // 恢复现场
                }
            }
        };

        dfs(0);
        return ans;
    }
};
```
- **时间复杂度**：$O(n * 2^n)$。其中 `n` 为 `s` 的长度。答案的长度至多为逗号子集的个数，即 $O(2^n)$，因此会递归 $O(2^n)$ 次，再算上判断回文和加入答案时需要 $O(n)$ 的时间，所以时间复杂度为$O(n2^n)$。
- **空间复杂度**：`O(n)`

> to optimize

- **时间复杂度**：`O(...)`
- **空间复杂度**：`O(...)`

>✨ tips

> others' answer

- **时间复杂度**：`O(...)`
- **空间复杂度**：`O(...)`

>✨ tips

## 🔁 复盘与扩展
> 关键点：

> 易错点：

## 🔗 相关题目
[[78. 子集]]

> ⏱️ 本次耗时：__30__ 分钟
