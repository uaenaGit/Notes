---
难度: easy
情况: Y(通过)
日期: 2026-03-12 19:17
分类: 递归, 树
tags:
  - leetcode
备注: 递归
created: 2026-03-12T19:17
updated: 2026-03-12T19:30
---

> 🔗[104 - 二叉树的最大深度](https://leetcode.cn/problems/maximum-depth-of-binary-tree/description/)

## 📌 题目描述
> （请在此处粘贴题目描述）

给定一个二叉树 `root` ，返回其最大深度。

二叉树的 **最大深度** 是指从根节点到最远叶子节点的最长路径上的节点数。

**示例 1：**

![](https://assets.leetcode.com/uploads/2020/11/26/tmp-tree.jpg)

**输入：** root = [3,9,20,null,null,15,7]
**输出：** 3

**示例 2：**

**输入：** root = [1,null,2]
**输出：** 2

**提示：**

- 树中节点的数量在 $[0, 10^4]$ 区间内。
- `-100 <= Node.val <= 100`

## 💡 解题思路 
![image.png](https://cdn.jsdelivr.net/gh/uaenaGit/image-host@main/images20260312192334686.png)

## 💻 代码实现
> my answer
```cpp
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode() : val(0), left(nullptr), right(nullptr) {}
 *     TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
 *     TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}
 * };
 */
class Solution {
public:
    int maxDepth(TreeNode* root) {
        if (root == nullptr) {
            return 0;
        }
        int l_depth = maxDepth(root->left);
        int r_depth = maxDepth(root->right);
        return max(l_depth, r_depth) + 1;
    }
};
```
- **时间复杂度**：`O(n)`。$n$ 时整棵树的节点个数。
- **空间复杂度**：`O(n)`。单支树，会退化成一个链表，递归需要 $O(n)$的栈空间。

> to optimize

自顶向下，记录节点深度。
```cpp
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode() : val(0), left(nullptr), right(nullptr) {}
 *     TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
 *     TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}
 * };
 */
class Solution {
public:
    int maxDepth(TreeNode* root) {
        int ans = 0;

        auto dfs = [&](this auto &&dfs, TreeNode* root, int depth) ->void {
            if (root == nullptr) {
                return;
            }
            ans = max(ans, depth);
            // 每往下一层，深度+1
            dfs(root->left, depth + 1);
            dfs(root->right, depth + 1);
        };

        // 根节点深度为1
        dfs(root, 1);
        return ans;
    }
};
```
- **时间复杂度**：`O(n)`
- **空间复杂度**：`O(n)`

>✨ tips

> others' answer

- **时间复杂度**：`O(...)`
- **空间复杂度**：`O(...)`

>✨ tips

## 🔁 复盘与扩展
> 关键点：

> 易错点：

## 🔗 相关题目


> ⏱️ 本次耗时：____ 分钟
