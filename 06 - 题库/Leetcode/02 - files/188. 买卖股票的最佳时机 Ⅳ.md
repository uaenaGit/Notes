---
难度: hard
情况: N(不会)
日期: 2026-03-02 14:57
分类: 动态规划, 数组
tags:
  - leetcode
备注: 增加交易次数限制
created: 2026-03-02T14:55
updated: 2026-03-02T22:40
---

> 🔗[188 - 买卖股票的最佳时机 Ⅳ](https://leetcode.cn/problems/best-time-to-buy-and-sell-stock-iv/)

## 📌 题目描述
> （请在此处粘贴题目描述）

给你一个整数数组 `prices` 和一个整数 `k` ，其中 `prices[i]` 是某支给定的股票在第 `i` 天的价格。

设计一个算法来计算你所能获取的最大利润。你最多可以完成 `k` 笔交易。也就是说，你最多可以买 `k` 次，卖 `k` 次。

**注意：** 你不能同时参与多笔交易（你必须在再次购买前出售掉之前的股票）。

**示例 1：**

**输入：** k = 2, prices = [2,4,1]
**输出：** 2
**解释：** 在第 1 天 (股票价格 = 2) 的时候买入，在第 2 天 (股票价格 = 4) 的时候卖出，这笔交易所能获得利润 = 4-2 = 2 。

**示例 2：**

**输入：** k = 2, prices = [3,2,6,5,0,3]
**输出：** 7
**解释：** 在第 2 天 (股票价格 = 2) 的时候买入，在第 3 天 (股票价格 = 6) 的时候卖出, 这笔交易所能获得利润 = 6-2 = 4 。随后，在第 5 天 (股票价格 = 0) 的时候买入，在第 6 天 (股票价格 = 3) 的时候卖出, 这笔交易所能获得利润 = 3-0 = 3 。

**提示：**

- `1 <= k <= 100`
- `1 <= prices.length <= 1000`
- `0 <= prices[i] <= 1000`

## 💡 解题思路 
思路如下：
![image.png](https://cdn.jsdelivr.net/gh/uaenaGit/image-host@main/images20260302153402315.png)

![image.png](https://cdn.jsdelivr.net/gh/uaenaGit/image-host@main/images20260302151952967.png)

## 💻 代码实现
> my answer
```cpp
class Solution {
public:
    int memo[1010][110][2];
    int dfs(int i, int j, int hold, vector<int>& prices) {
        if (j < 0) {
            return INT_MIN / 2; // memo[*, -1, *] = -∞
        }
        if (i < 0) {
            return hold ? INT_MIN / 2 : 0;
        }
        int &res = memo[i][j][hold];
        if (res != INT_MIN) {
            return res;
        }
        if (hold) {
            return res = max(dfs(i - 1, j, 1, prices), dfs(i - 1, j, 0, prices) - prices[i]);
        }
        return res = max(dfs(i - 1, j, 0, prices), dfs(i - 1, j - 1, 1, prices) + prices[i]);
    }
    int maxProfit(int k, vector<int>& prices) {
        int n = prices.size();
        // memo数组初始化
        for (int i = 0; i < n; i++) {
            for (int j = 0; j < k + 1; j++) {
                memo[i][j][0] = INT_MIN;
                memo[i][j][1] = INT_MIN;
            }
        }
        return dfs(n - 1, k, 0, prices);
    }
};
```
- **时间复杂度**：`O(nk)`。$n$ 是`prices`数组长度。
- **空间复杂度**：`O(nk)`

> to optimize

翻译成递推。
![image.png](https://cdn.jsdelivr.net/gh/uaenaGit/image-host@main/images20260302213705269.png)
注意：
- 在记忆化搜索中，`j` 的范围是 `[−1,k]`，这一共有 `k+2` 个数。$1:1$ 翻译成递推就需要 `k+2` 的数组大小。
- `f` 数组中 `j = 0` 对应着记忆化搜索中交易 `-1` 次的状态。不合法，初值为`-∞`
- `f`的初始值来自记忆化搜索的边界。

```cpp
class Solution {
public:
    int maxProfit(int k, vector<int>& prices) {
        int n = prices.size();
        // 三维vector初始化
        vector<vector<vector<int>>> f(n + 1, vector<vector<int>> (k + 2, vector<int>(2, INT_MIN / 2)));
        f[0][0][0] = INT_MIN, f[0][0][1] = INT_MIN;
        for (int j = 1; j <= k + 1; j++) f[0][j][0] = 0;
        for (int i = 0; i < n; i++) {
            // 注意 j 是从 1 开始计算 0为非法
            for (int j = 1; j <= k + 1; j++) {
                f[i + 1][j][0] = max(f[i][j][0], f[i][j - 1][1] + prices[i]);
                f[i + 1][j][1] = max(f[i][j][1], f[i][j][0] - prices[i]);
            }
        }
        // 由于多插入了一个状态，所以返回 f[n][k + 1][0]
        return f[n][k + 1][0];
    }
};
```

- **时间复杂度**：`O(nk)`
- **空间复杂度**：`O(nk)`

对状态数组进行空间优化。
- 由于 `i + 1` 的状态只依赖于`i` 的状态，所以直接删除第一个维度
```cpp
class Solution {
public:
    int maxProfit(int k, vector<int>& prices) {
        int n = prices.size();
        vector<vector<int>> f(k + 2, vector<int>(2, INT_MIN / 2));
        for (int j = 1; j <= k + 1; j++) f[j][0] = 0;
        for (int i = 0; i < n; i++) {
            // 注意 j 是从 1 开始计算 0为非法
            for (int j = 1; j <= k + 1; j++) {
                f[j][0] = max(f[j][0], f[j - 1][1] + prices[i]);
                f[j][1] = max(f[j][1], f[j][0] - prices[i]);
            }
        }
        // 由于多插入了一个状态，所以返回 f[n][k + 1][0]
        return f[k + 1][0];
    }
};
```

- **时间复杂度**：`O(nk)`
- **空间复杂度**：`O(k)`

>✨ tips
- 在 `f` 的前面多插入一个状态。
> others' answer
![image.png](https://cdn.jsdelivr.net/gh/uaenaGit/image-host@main/images20260302223938875.png)

- **时间复杂度**：`O(...)`
- **空间复杂度**：`O(...)`

>✨ tips

## 🔁 复盘与扩展
> 关键点：

> 易错点：

## 🔗 相关题目


> ⏱️ 本次耗时：__80__ 分钟
