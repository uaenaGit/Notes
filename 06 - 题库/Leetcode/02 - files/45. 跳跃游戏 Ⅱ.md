---
难度: medium
情况: Y(通过)
日期: 2026-03-03 10:09
分类: 贪心
tags:
  - 面试经典150题
备注: 区间贪心
created: 2026-03-03T10:08
updated: 2026-03-22T10:23
---

> 🔗[45 - 跳跃游戏 Ⅱ](https://leetcode.cn/problems/jump-game-ii/description/?envType=study-plan-v2&envId=top-interview-150)

## 📌 题目描述
> （请在此处粘贴题目描述）

给定一个长度为 `n` 的 **0 索引**整数数组 `nums`。初始位置在下标 0。

每个元素 `nums[i]` 表示从索引 `i` 向后跳转的最大长度。换句话说，如果你在索引 `i` 处，你可以跳转到任意 `(i + j)` 处：

- `0 <= j <= nums[i]` 且
- `i + j < n`

返回到达 `n - 1` 的最小跳跃次数。测试用例保证可以到达 `n - 1`。

**示例 1:**

**输入:** nums = [2,3,1,1,4]
**输出:** 2
**解释:** 跳到最后一个位置的最小跳跃数是 `2`。
     从下标为 0 跳到下标为 1 的位置，跳 `1` 步，然后跳 `3` 步到达数组的最后一个位置。

**示例 2:**

**输入:** nums = [2,3,0,1,4]
**输出:** 2

**提示:**

- $1 <= nums.length <= 10^4$
- `0 <= nums[i] <= 1000`
- 题目保证可以到达 `n - 1`

## 💡 解题思路 
使用`dis`数组预处理到每个下标`i` 所需的最小跳跃次数。显然当`i = 0`时，`dis[0] = 0`。
- 只有当下标`pos = i + j`（*`j` 是 `nums[i]`*）没被更新的时候才使用`dis[i] + 1`更新 `dis[pos]`。
- 答案返回 `dis[n - 1]`即可

## 💻 代码实现
> my answer
```cpp
class Solution {
public:
    int jump(vector<int>& nums) {
        int n = nums.size();
        vector<int> dis(n + 1, 0); // 初始化 n + 1维的一维数组
        // 预处理到每个下标i的最少跳转次数
        for (int i = 0; i < n; i++) {
            for (int j = 1; j <= nums[i]; j++) {
                int pos = i + j;
                if ((pos <= n) && (dis[pos] == 0)) {
                    dis[pos] = dis[i] + 1;
                }
            }
        }
        
        return dis[n - 1];
    }
};
```
- **时间复杂度**：`O(nk)`。$n$ 是`nums`数组长度，$k$ 是数组元素最大值。
- **空间复杂度**：`O(n)`。$n$ 是`nums`数组长度。

> to optimize

![image.png](https://cdn.jsdelivr.net/gh/uaenaGit/image-host@main/images20260322102313843.png)

使用`next_right`和`cur_right`维护所能到达的最远距离和当前的位置。遍历数组`nums`，若`i == cur_right`则`ans++`，同时更新`cur_right = next_right`。由于一定能到达`n - 1`，所以遍历到`n - 2`即可。
```cpp
class Solution {
public:
    int jump(vector<int>& nums) {
        int n = nums.size();
        int ans = 0;
        /*
        cur_right 表示已建造桥的右端点
        next_right 表示下一座桥的右端点的最大值
        */
        int cur_right = 0, next_right = 0;
        for (int i = 0; i + 1 < nums.size(); i++) {
            next_right = max(next_right, i + nums[i]);
            if (i == cur_right) { // 无路可走必须建桥
                ans++;
                cur_right = next_right;
            }
        }
        return ans;
    }
};
```
- **时间复杂度**：`O(n)`。$n$ 是`nums`数组。
- **空间复杂度**：`O(1)`

>✨ tips

> others' answer

- **时间复杂度**：`O(...)`
- **空间复杂度**：`O(...)`

>✨ tips

## 🔁 复盘与扩展
> 关键点：
- 题目保证一定能到到`n - 1`位置，所以只需要遍历到`n - 2` 即可。
> 易错点：

## 🔗 相关题目
[[1326. 灌溉花园的最少水龙头数目]]

> ⏱️ 本次耗时：__30__ 分钟
