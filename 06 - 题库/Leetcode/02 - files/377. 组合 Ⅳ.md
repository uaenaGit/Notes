---
难度: medium
情况: ?(有疑问)
日期: 2026-03-15 23:49
分类: 动态规划
tags:
  - leetcode
备注: 记忆化搜索；递推
created: 2026-03-15T23:49
updated: 2026-03-16T00:06
---

> 🔗[377 - 组合 Ⅳ](https://leetcode.cn/problems/combination-sum-iv/description/)

## 📌 题目描述
> （请在此处粘贴题目描述）

给你一个由 **不同** 整数组成的数组 `nums` ，和一个目标整数 `target` 。请你从 `nums` 中找出并返回总和为 `target` 的元素排列的个数。

题目数据保证答案符合 32 位整数范围。

**示例 1：**

**输入：** nums = [1,2,3], target = 4
**输出：** 7
**解释：**
所有可能的组合为：
(1, 1, 1, 1)
(1, 1, 2)
(1, 2, 1)
(1, 3)
(2, 1, 1)
(2, 2)
(3, 1)
请注意，顺序不同的序列被视作不同的组合。

**示例 2：**

**输入：** nums = [9], target = 3
**输出：** 0

**提示：**

- `1 <= nums.length <= 200`
- `1 <= nums[i] <= 1000`
- `nums` 中的所有元素 **互不相同**
- `1 <= target <= 1000`

## 💡 解题思路 
思路类似于[[70. 爬楼梯]]。将`target`看成总的楼梯数，将`nums`中的元素看成每次可爬的楼梯数。

## 💻 代码实现
> my answer

记忆化搜索。
```cpp
class Solution {
public:
    int combinationSum4(vector<int>& nums, int target) {
        int n = nums.size();
        vector<int> memo(target + 1, -1);

        auto dfs = [&](this auto &&dfs, int i) ->int {
            if (i == 0) { // 爬完了
                return 1;
            }
            int &res = memo[i];
            if (res != -1) {
                return res; // 被计算过
            }
            res = 0; // 因为原来的值是-1但是方案数应该从0开始累加
            for (auto &x : nums) {
                if (i >= x) { // 注意i >= x，即至少剩下的楼梯数要不小于当前的可爬楼梯数
                    res += dfs(i - x);
                }
            }
            return res;

        };
        return dfs(target);
    }
};

// 也可以这样写，但是不如上面的优雅
class Solution {
public:
    int combinationSum4(vector<int>& nums, int target) {
        int n = nums.size();
        vector<int> memo(target + 1, -1);

        auto dfs = [&](this auto &&dfs, int i) ->int {
            if (i == 0) { // 爬完了
                return 1;
            }
            if (memo[i] != -1) {
                return memo[i]; // 被计算过
            }
            // 使用变量记录方案数
            int res = 0;
            for (auto &x : nums) {
                if (i >= x) { // 注意i >= x，即至少剩下的楼梯数要不小于当前的可爬楼梯数
                    res += dfs(i - x);
                }
            }
            memo[i] = res;
            return res;

        };
        return dfs(target);
    }
};
```
- **时间复杂度**：$O(n * target)$。`n`是`nums`长度， 本题状态个数为$O(target)$，单个状态计算时间为 $O(n)$。时间复杂度 = 状态个数 × 单个状态的计算时间。
- **空间复杂度**：$O(target)$。有多少个状态，`memo`数组的大小就是多少。

> to optimize

$1 : 1$ 翻译成递推。
```cpp
class Solution {
public:
    int combinationSum4(vector<int>& nums, int target) {
        int n = nums.size();
        // 使用unsigned int 防止溢出(题目)
        vector<unsigned int> f(target + 1, 0); // 初始化为0
        f[0] = 1;
        for (int i = 1; i <= target; i++) {
            for (auto &x : nums) {
                if (i >= x) {
                    f[i] += f[i - x];
                }
            }
        }
        return f[target];
    }
};
```
- **时间复杂度**：$O(n * target)$
- **空间复杂度**：$O(target)$

>✨ tips

> others' answer

- **时间复杂度**：$O(...)$
- **空间复杂度**：$O(...)$

>✨ tips

## 🔁 复盘与扩展
> 关键点：

> 易错点：

> 变形与扩展：

## 🔗 相关题目


> ⏱️ 本次耗时：__30__ 分钟
