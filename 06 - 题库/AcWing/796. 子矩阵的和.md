---
难度: 简单
情况: Y(通过)
日期: 2026-03-09 17:04
分类: 数组
tags:
  - acwing
备注: 二维前缀和
created: 2026-03-09T17:04
updated: 2026-03-09T17:20
---

> 🔗[796 - 子矩阵的和](https://www.acwing.com/problem/content/798/)

## 📌 题目描述
> （请在此处粘贴题目描述）

输入一个 n 行 m 列的整数矩阵，再输入 q 个询问，每个询问包含四个整数 x1,y1,x2,y2，表示一个子矩阵的左上角坐标和右下角坐标。

对于每个询问输出子矩阵中所有数的和。

#### 输入格式

第一行包含三个整数 n，m，q。

接下来 n 行，每行包含 m 个整数，表示整数矩阵。

接下来 q 行，每行包含四个整数 x1,y1,x2,y2，表示一组询问。

#### 输出格式

共 q 行，每行输出一个询问的结果。

#### 数据范围

1≤n,m≤1000,  
1≤q≤200000,  
1≤x1≤x2≤n,  
1≤y1≤y2≤m,  
−1000≤矩阵内元素的值≤1000

#### 输入样例：

```
3 4 3
1 7 2 4
3 6 2 8
2 1 2 3
1 1 2 2
2 1 3 4
1 3 3 4
```

#### 输出样例：

```
17
27
21
```

## 💡 解题思路 
![image.png](https://cdn.jsdelivr.net/gh/uaenaGit/image-host@main/images20260309171646709.png)

```cpp
//s[i, j] = 第i行j列格子左上部分所有元素的和 = s[i-1, j] + s[i, j-1] - s[i-1][j-1] + a[i][j]

//预处理
s[i][j] = s[i-1][j] + s[i][j-1] - s[i-1][j-1] + a[i][j];

//以(x1, y1)为左上角，(x2, y2)为右下角的子矩阵的和
s[x2][y2] - s[x1-1][y2] - s[x2][y1-1] + s[x1-1][y1-1]; 
```
## 💻 代码实现
> my answer
```cpp
#include <iostream>

using namespace std;

const int N = 1010;
int a[N][N], s[N][N]; // a是原数组 s是前缀和数组

int main()
{
    int n, m, q;
    scanf("%d%d%d", &n, &m, &q);
    
    for (int i = 1; i <= n; i++) {
        for (int j = 1; j <= m; j++) {
            scanf("%d", &a[i][j]);
        }
    }
    
    // 预处理
    for (int i = 1; i <= n; i++) {
        for (int j = 1; j <= m; j++) {
            s[i][j] = s[i-1][j] + s[i][j-1] - s[i-1][j-1] + a[i][j];
        }
    }
    
    while (q--) {
        int x1, y1, x2, y2;
        scanf("%d%d%d%d", &x1, &y1, &x2, &y2);
        int res = s[x2][y2] - s[x1-1][y2] - s[x2][y1-1] + s[x1-1][y1-1];
        printf("%d\n", res);
    }
    
    return 0;
}
```

- **时间复杂度**：$O(n ^ 2)$
- **空间复杂度**：$O(n^2)$

> to optimize

- **时间复杂度**：`O(...)`
- **空间复杂度**：`O(...)`

>✨ tips

> others' answer

- **时间复杂度**：`O(...)`
- **空间复杂度**：`O(...)`

>✨ tips

## 🔁 复盘与扩展
> 关键点：

> 易错点：

## 🔗 相关题目


> ⏱️ 本次耗时：__15__ 分钟
