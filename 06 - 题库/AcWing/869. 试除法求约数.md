---
难度: 简单
情况: Y(通过)
日期: 2026-03-09 21:18
分类: 数学
tags:
  - acwing
备注: 求约数
created: 2026-03-09T21:18
updated: 2026-03-21T17:44
---

> 🔗[869 - 试除法求约数](https://www.acwing.com/problem/content/871/)

## 📌 题目描述
> （请在此处粘贴题目描述）

给定 n 个正整数 $a_i$，对于每个整数 $a_i$，请你按照从小到大的顺序输出它的所有约数。

#### 输入格式

第一行包含整数 n。

接下来 n 行，每行包含一个整数 $a_i$。

#### 输出格式

输出共 n 行，其中第 i 行输出第 i 个整数 $a_i$ 的所有约数。

#### 数据范围

1≤n≤100,  
$1 ≤ a_i ≤ 2×10^9$

#### 输入样例：

```
2
6
8
```

#### 输出样例：

```
1 2 3 6 
1 2 4 8 
```

## 💡 解题思路 
求约数——试除法
注意：约数一定成对出现，如果 $d | n$ ，那么 $\frac {n} {d} | n$ 只需要枚举到 $d \leq \frac {n} {d}$ 即 $d \leq \sqrt n$ 即可。

## 💻 代码实现
> my answer
```cpp
#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

//求n的所有约数
vector<int> get_divisors(int n)
{
    vector<int> res;
    
	// i <= n/i 而不写 i*i <= n 是为了保证不溢出
    for(int i = 1; i <= n / i; i++)
    {
        if(n % i == 0) 
        {
            res.push_back(i);
			
			// 当较小的约数不等于较大的约数时才加入res
            if(i != n / i) res.push_back(n / i);
        }
    }
    
    sort(res.begin(), res.end()); //排序
    return res;
}

int main()
{
    int n;
    cin >> n;
    vector<int> res;
    while (n--) {
        int x;
        cin >> x;
        res = get_divisors(x);
        for (auto &num : res) cout << num << ' ';
        cout << '\n';
    }
    
    return 0;
}
```
- **时间复杂度**：$O(\sqrt n)$。
- **空间复杂度**：`O(1)`

> to optimize

- **时间复杂度**：`O(...)`
- **空间复杂度**：`O(...)`

>✨ tips

> others' answer

- **时间复杂度**：`O(...)`
- **空间复杂度**：`O(...)`

>✨ tips

## 🔁 复盘与扩展
> 关键点：

> 易错点：

## 🔗 相关题目


> ⏱️ 本次耗时：____ 分钟
