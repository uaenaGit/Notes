---
created: 2026-02-27T11:46
updated: 2026-02-27T11:46
---
这是一份为您精心整理的 **C++ `std::unordered_set` 全方位方法详解**。

`std::unordered_set` 是基于**哈希表**（Hash Table）实现的**无序、去重**关联容器。它的核心优势是**极快的查找速度**（平均 $O(1)$），但元素**不保持顺序**。

这份指南严格遵循 C++11 及以上标准，**重点补充了每个方法的详细参数说明、返回值含义以及具体的代码示例**。

---

# 📚 C++ `std::unordered_set` 完全指南 (C++11+)

### 📌 前置准备
```cpp
#include <iostream>
#include <unordered_set>
#include <vector>
#include <string>

using namespace std;
```

> **核心特性速览**：
> 1. **无序存储**：元素遍历顺序与插入顺序无关，也不按大小排序。
> 2. **唯一性**：自动去重，重复插入无效。
> 3. **极速查找**：平均时间复杂度为 **$O(1)$**，最坏情况 $O(N)$（哈希冲突严重时）。
> 4. **不可修改**：元素是 `const` 的，不能直接修改值。
> 5. **自定义哈希**：支持自定义哈希函数，可存储任意类型。

---

## 1️⃣ 构造与模板定义 (Construction & Template)
`unordered_set` 的模板定义比 `set` 多了一个哈希函数参数。

| 方法/定义 | 参数详解 | 返回值 | 功能说明 |
| :--- | :--- | :--- | :--- |
| **`template<...>`** | `Key`: 元素类型`Hash`: 哈希函数对象 (默认 `hash<Key>`)`KeyEqual`: 键比较函数 (默认 `equal_to<Key>`)`Alloc`: 分配器 | - | 定义类型。 |
| **`unordered_set()`** | 无 | - | 默认构造，生成空集合。 |
| **`unordered_set(InputIt first, InputIt last)`** | `first`, `last`: 迭代器范围 | - | 范围构造，复制元素并去重。 |
| **`unordered_set(initializer_list<T> init)`** | `init`: 初始化列表 (C++11) | - | 列表构造，如 `{1, 2, 3}`。 |
| **`unordered_set(size_t bucket_count)`** | `bucket_count`: 初始桶数量 | - | 指定初始哈希桶数量，可减少早期扩容开销。 |
| **`unordered_set(size_t n, const Hash& hash)`** | `n`: 桶数量`hash`: 自定义哈希函数 | - | 指定桶数量和自定义哈希函数。 |

### 💻 代码示例
```cpp
int main() {
    // 1. 默认构造
    unordered_set<int> s1;
    
    // 2. 初始化列表 (无序)
    unordered_set<int> s2{5, 2, 9, 2, 1}; 
    // 内容可能是 {1, 2, 5, 9} 或其他顺序，取决于哈希
    
    // 3. 指定桶数量 (优化性能)
    unordered_set<string> s3(100); 
    
    // 4. 范围构造
    vector<int> v = {10, 20, 30};
    unordered_set<int> s4(v.begin(), v.end());
    
    cout << "s2 size: " << s2.size() << endl; // 4
    return 0;
}
```

---

## 2️⃣ 容量与状态 (Capacity & State)
除了常规大小查询，`unordered_set` 还提供了特有的**桶**（Bucket）相关查询，用于监控哈希表性能。

| 方法 | 参数详解 | 返回值 | 功能说明 |
| :--- | :--- | :--- | :--- |
| **`size()`** | 无 | `size_t` | 返回元素个数。 |
| **`empty()`** | 无 | `bool` | 若为空返回 `true`。 |
| **`max_size()`** | 无 | `size_t` | 理论最大容量。 |
| **`clear()`** | 无 | `void` | 清空所有元素。 |
| **`bucket_count()`** | 无 | `size_t` | 返回当前哈希桶的总数。 |
| **`load_factor()`** | 无 | `float` | 返回负载因子 (`size / bucket_count`)。过高会导致性能下降。 |
| **`max_load_factor()`** | 无 | `float` | 返回最大允许负载因子 (默认通常为 1.0)。 |
| **`max_load_factor(float z)`** | `z`: 新阈值 | `void` | 设置最大负载因子。超过此值会自动扩容。 |
| **`reserve(n)`** | `n`: 期望元素个数 | `void` | **重要**：预留足够空间以容纳 `n` 个元素而不触发扩容。 |
| **`rehash(n)`** | `n`: 新桶数量 | `void` | 强制重新哈希，设置桶数量至少为 `n`。 |

### 💡 性能提示
- 当 `load_factor()` 接近 `max_load_factor()` 时，哈希冲突增加，查找变慢。
- 使用 `reserve(n)` 可以避免在大量插入过程中频繁 rehash，显著提升性能。

### 💻 代码示例
```cpp
int main() {
    unordered_set<int> s;
    
    // 预分配空间，避免多次 rehash
    s.reserve(1000); 
    
    for (int i = 0; i < 100; ++i) s.insert(i);
    
    cout << "Size: " << s.size() << endl;
    cout << "Buckets: " << s.bucket_count() << endl;
    cout << "Load Factor: " << s.load_factor() << endl;
    
    // 强制调整桶数量
    s.rehash(200); 
    cout << "New Buckets: " << s.bucket_count() << endl;
    
    return 0;
}
```

---

## 3️⃣ 元素操作 (Element Operations)
增删查改的核心逻辑与 `set` 非常相似，但**没有**有序查找方法（如 `lower_bound`）。

| 方法 | 参数详解 | 返回值 | 功能说明 |
| :--- | :--- | :--- | :--- |
| **`insert(val)`** | `val`: 要插入的值 | `pair<iterator, bool>` | 插入元素。`bool`: `true`=成功，`false`=已存在。`iterator`: 指向该元素的迭代器。 |
| **`emplace(args...)`** | `args`: 构造参数的列表 (C++11) | `pair<iterator, bool>` | **原地构造**。直接在哈希表中构造元素，避免拷贝/移动开销。 |
| **`erase(val)`** | `val`: 目标值 | `size_t` | 删除值为 `val` 的元素。返回 0 或 1。 |
| **`erase(it)`** | `it`: 迭代器 | `iterator` (C++11) | 删除迭代器指向的元素。返回下一个迭代器。 |
| **`erase(first, last)`** | `first`, `last`: 迭代器范围 | `iterator` | 删除范围内的元素。 |
| **`count(val)`** | `val`: 目标值 | `size_t` | 统计个数。对于 `unordered_set`，返回 **0** 或 **1**。常用于判断存在性（比 `find` 更简洁）。 |
| **`find(val)`** | `val`: 目标值 | `iterator` | 查找元素。找到返回迭代器，未找到返回 `end()`。**平均 $O(1)$**。 |
| **`swap(unordered_set& other)`** | `other`: 另一个集合 | `void` | 交换内容 ($O(1)$)。 |

> ⚠️ **注意**：同样不能通过迭代器修改元素值（`*it = new_val` 非法）。

### 💻 代码示例
```cpp
int main() {
    unordered_set<string> s;
    
    // 1. 插入
    auto ret = s.insert("Apple");
    if (ret.second) cout << "Inserted Apple\n";
    
    // 2. emplace (高效构造)
    s.emplace("Banana"); // 等价于 insert("Banana")，但对于复杂对象更高效
    
    // 3. 查找与计数
    if (s.count("Apple")) {
        cout << "Apple exists!\n";
    }
    
    auto it = s.find("Banana");
    if (it != s.end()) {
        cout << "Found: " << *it << "\n";
    }
    
    // 4. 删除
    s.erase("Apple");       // 按值删除
    s.erase(it);            // 按迭代器删除
    
    // 5. 遍历删除模式
    s.insert({"A", "B", "C"});
    for (auto it = s.begin(); it != s.end(); ) {
        if (*it == "B") {
            it = s.erase(it);
        } else {
            ++it;
        }
    }
    
    return 0;
}
```

---

## 4️⃣ 桶接口 (Bucket Interface) - 特有功能
这些方法是 `unordered_set` 独有的，用于底层调试或极致优化。

| 方法 | 参数详解 | 返回值 | 功能说明 |
| :--- | :--- | :--- | :--- |
| **`bucket(key)`** | `key`: 元素值 | `size_t` | 返回指定元素所在的**桶索引** (0 到 `bucket_count()-1`)。 |
| **`bucket_size(n)`** | `n`: 桶索引 | `size_t` | 返回第 `n` 个桶中的元素个数（用于检测哈希冲突）。 |
| **`begin(n)`** | `n`: 桶索引 | `local_iterator` | 返回第 `n` 个桶的起始迭代器。 |
| **`end(n)`** | `n`: 桶索引 | `local_iterator` | 返回第 `n` 个桶的结束迭代器。 |

### 💻 代码示例
```cpp
int main() {
    unordered_set<int> s{10, 20, 30};
    
    // 查看元素 20 在哪个桶
    size_t idx = s.bucket(20);
    cout << "20 is in bucket: " << idx << endl;
    
    // 查看该桶有多少元素（冲突数）
    cout << "Bucket size: " << s.bucket_size(idx) << endl;
    
    // 遍历特定桶内的元素
    cout << "Elements in bucket " << idx << ": ";
    for (auto local_it = s.begin(idx); local_it != s.end(idx); ++local_it) {
        cout << *local_it << " ";
    }
    cout << endl;
    
    return 0;
}
```

---

## 5️⃣ 自定义哈希函数 (Custom Hash)
默认哈希支持 `int`, `string`, `double` 等基础类型。如果要存**自定义结构体**，必须提供哈希函数。

### 方法 A：定义仿函数 (推荐)
```cpp
struct Person {
    string name;
    int age;
    // 需要重载 == 用于判断键相等
    bool operator==(const Person& other) const {
        return name == other.name && age == other.age;
    }
};

// 自定义哈希结构体
struct PersonHash {
    size_t operator()(const Person& p) const {
        // 组合多个成员的哈希值
        // C++17 推荐使用 hash_combine 或类似技巧，这里用简单异或
        return hash<string>()(p.name) ^ (hash<int>()(p.age) << 1);
    }
};

int main() {
    // 声明时传入自定义哈希类型
    unordered_set<Person, PersonHash> people;
    
    people.insert({"Alice", 25});
    people.insert({"Bob", 30});
    
    if (people.count({"Alice", 25})) {
        cout << "Alice found!" << endl;
    }
    
    return 0;
}
```

### 方法 B：特化 `std::hash` (全局生效)
```cpp
namespace std {
    template<>
    struct hash<Person> {
        size_t operator()(const Person& p) const {
            return hash<string>()(p.name) ^ (hash<int>()(p.age) << 1);
        }
    };
}
// 之后可以直接用 unordered_set<Person> s;
```

---

## 6️⃣ 迭代与比较 (Iteration & Comparison)

| 方法 | 参数详解 | 返回值 | 功能说明 |
| :--- | :--- | :--- | :--- |
| **`begin()` / `end()`** | 无 | `iterator` | 正向迭代器。**遍历顺序是随机的**（取决于哈希值和桶分布）。 |
| **`cbegin()` / `cend()`** | 无 | `const_iterator` | 只读迭代器。 |
| **`operator==`** | `rhs`: 另一个 unordered_set | `bool` | 比较两个集合是否包含完全相同的元素（顺序无关）。 |

> ⚠️ **警告**：**不要**依赖 `unordered_set` 的遍历顺序！每次运行程序，甚至同一程序的不同次插入，遍历顺序都可能不同。如果需要有序遍历，请将数据复制到 `vector` 后排序，或使用 `std::set`。

### 💻 代码示例
```cpp
int main() {
    unordered_set<int> s{5, 1, 9, 2};
    
    // 遍历 (顺序不确定，可能是 1 9 2 5 或其他)
    cout << "Random Order: ";
    for (int x : s) {
        cout << x << " ";
    }
    cout << endl;
    
    // 比较 (顺序无关，只要元素相同即相等)
    unordered_set<int> s2{1, 2, 5, 9};
    cout << "Equal? " << (s == s2) << endl; // 1 (true)
    
    return 0;
}
```

---

## 7️⃣ `unordered_set` vs `set` vs `vector` 终极对比

| 特性        | `std::unordered_set`  | `std::set`           | `std::vector`                   |
| :-------- | :-------------------- | :------------------- | :------------------------------ |
| **底层结构**  | **哈希表**               | **红黑树**              | 动态数组                            |
| **元素顺序**  | **无序** (随机)           | **有序** (自动排序)        | 插入顺序 (需手动 sort)                 |
| **唯一性**   | 唯一                    | 唯一                   | 可重复                             |
| **查找复杂度** | **平均 $O(1)$** ⚡       | $O(\log N)$          | $O(N)$ (未排序) / $O(\log N)$ (二分) |
| **插入/删除** | **平均 $O(1)$**         | $O(\log N)$          | $O(N)$ (中间操作)                   |
| **范围查询**  | ❌ 不支持                 | ✅ 支持 (`lower_bound`) | ✅ 支持 (需排序)                      |
| **内存占用**  | 较高 (为了减少冲突，桶多)        | 高 (指针开销)             | 最低                              |
| **适用场景**  | **海量数据快速去重/查找**，不需要顺序 | 需要**有序**、范围查找、前驱后继   | 需要**下标访问**、缓存友好、数据量小            |

---

### 💡 核心总结
1. **速度优先**：如果你只需要判断“元素是否存在”或“去重”，且数据量大，**首选 `unordered_set`**。
2. **无需顺序**：千万不要依赖它的遍历顺序。如果需要有序输出，请拷贝到 `vector` 后 `sort`。
3. **自定义类型**：存自定义对象时，**必须**提供哈希函数 (`operator()`) 和相等判断 (`operator==`)。
4. **性能调优**：已知数据量大小时，使用 `reserve()` 预分配空间，避免频繁 rehash 导致性能抖动。
5. **陷阱**：最坏情况下（所有元素哈希冲突），性能退化为 $O(N)$。设计良好的哈希函数至关重要。

**一句话口诀**：
> “查重要快用无序，哈希冲突要留意；
> 若要排序转 Set，预留空间 reserve 起。”