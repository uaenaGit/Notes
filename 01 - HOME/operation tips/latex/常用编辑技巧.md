---
created: 2026-03-05T10:21
updated: 2026-05-12T10:08
---
```
1. 公式换行
$$
\begin{align}
    a &= b + c \\
      &= d + e + f
\end{align}
$$
```
$$
\begin{align}
    a &= b + c \\
      &= d + e + f
\end{align}
$$
```
2. s_k \ equiv 0 \pmod{m}
```
$$
s_k \equiv 0 \pmod{m} \\
$$
```
3. a \bmod b
```
$$
a \bmod b
$$
```
4. 
$$ 
dfs(i, j) = \begin{cases} dfs(i - 1, j - 1) + 1 & s[i] = t[j] \\ 
\max(dfs(i - 1, j), dfs(i, j - 1)) & s[i] \neq t[j] \end{cases} 
$$
```
$$ 
dfs(i, j) = \begin{cases} dfs(i - 1, j - 1) + 1 & s[i] = t[j] \\ \max(dfs(i - 1, j), dfs(i, j - 1)) & s[i] \neq t[j] \end{cases} 
$$

```
$$
\text{循环顺序}
\begin{cases} 
i < k, \ f[i] \text{ 从 } f[k] \text{ 转移过来，所以 } i \text{ 要} \color{red}{\text{倒序}} \text{枚举} \\ 
j > k, \ f[i][j] \text{ 从 } f[i][k] \text{ 转移过来，所以 } j \text{ 要} \color{blue}{\text{正序}} \text{枚举} 
\end{cases}
$$
```
$$
\text{循环顺序}
\begin{cases} 
i < k, \ f[i] \text{ 从 } f[k] \text{ 转移过来，所以 } i \text{ 要} \color{red}{\text{倒序}} \text{枚举} \\ 
j > k, \ f[i][j] \text{ 从 } f[i][k] \text{ 转移过来，所以 } j \text{ 要} \color{blue}{\text{正序}} \text{枚举} 
\end{cases}
$$
